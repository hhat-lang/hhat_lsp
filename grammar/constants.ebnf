(* ===================================================================
   H-hat Constants Grammar
   ===================================================================
   Depends on: core.ebnf (simple_id, type_id, expr)
   =================================================================== *)

consts                      = "const" simple_id ":" type_id "=" expr
